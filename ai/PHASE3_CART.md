# Phase 3 Cart Status

Superseded by the implemented `Commerce Core MVP`.

## Current Runtime
- Cart is implemented in `apps/api` and `apps/web`.
- Cart is persisted in Postgres via Prisma.
- Cart enforces a `single-seller` rule.
- Quantity updates and stock checks are handled in the API.

## Canonical References
- `README.md`
- `ai/architecture.md`
- `ai/tasks/backlog.md`
- `apps/api/src/cart`
- `apps/web/src/App.jsx`
